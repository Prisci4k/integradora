// Código JavaScript generado por Webpack
// Puede incluir código de tus componentes de React y otras dependencias

// Por ejemplo:
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
